/**
 * Public HtmlDom markup API: parser, document model, provider contract, scene loader and compiler facade.
 * Provider implementations live in dev.takesome.htmldom.markup.provider.
 */
package dev.takesome.htmldom.markup;
