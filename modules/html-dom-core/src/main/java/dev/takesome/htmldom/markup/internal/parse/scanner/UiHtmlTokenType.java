package dev.takesome.htmldom.markup.internal.parse.scanner;

/** Token classes produced by the HtmlDom HTML scanner. */
public enum UiHtmlTokenType {
    TEXT,
    START_TAG,
    END_TAG,
    RAW_TEXT,
    EOF,
    ERROR
}
