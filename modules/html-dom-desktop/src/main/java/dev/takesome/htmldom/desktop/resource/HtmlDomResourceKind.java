package dev.takesome.htmldom.desktop.resource;

/** Resource classes used by the desktop HtmlDom runtime. */
public enum HtmlDomResourceKind {
    MARKUP,
    CSS,
    SCRIPT,
    IMAGE,
    FONT,
    DEVTOOLS,
    BINARY
}
